<Window x:Class="HttpLinkTittles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HttpLinkTittles"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Button Content="Get Titles" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" Margin="10,10,0,0" VerticalAlignment="Top" Click="Button_Click"/>
        <TextBox x:Name="txtInputLinks" Grid.Row="1" Grid.Column="0" Margin="10,10,10,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AcceptsReturn="True" AcceptsTab="True" />

        <TextBox x:Name="txtStatus" Grid.Row="0" Grid.Column="1" Text="Output" HorizontalAlignment="Center" Margin="10,10,0,0" VerticalAlignment="Top" />
        <TextBox x:Name="txtOutputLinks" Grid.Row="1" Grid.Column="1" Margin="10,10,10,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AcceptsReturn="True" AcceptsTab="True" />


    </Grid>
</Window>
